//If then else complex nesting
IF(abc>=0)
{p=abc-1;
	IF(p<(sqrt abc))
	{}
	ELSE
	{ GO 10; JUMP 10; GO 10; p=0;}

	IF(p==0)
	THEN
	{GO 40; 
	TURN 90; 
	GO 40;
	IF(p+sin abc>=0) 
	{JUMP 100;}
	ELSE{}
	}	
	ELSE
	{
	IF (abc>=0)
	{JUMP 40;
	}}
}
ELSE{ GO 5; JUMP 10; GO 5;}
